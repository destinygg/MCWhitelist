enabled: true
whitelist:
  # the delay, in seconds, between the updating of the remote whitelist.
  delay: 60
  # grace period before server kicks non-whitelisted players
  grace: 60
  # The unique IDs of the whitelists to pull from.
  urls:
  - replace_me
dgg:
  baseUrl: https://stage.destiny.gg/auth/minecraft
  privateKey: privateKey